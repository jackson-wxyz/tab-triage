# Core dependencies — intentionally minimal.
# No PyTorch, no GPU libs. All LLM/embedding work goes through LM Studio's API.

requests>=2.28
trafilatura>=1.6
beautifulsoup4>=4.12
curl_cffi>=0.7       # Chrome TLS impersonation — bypasses Cloudflare bot detection
